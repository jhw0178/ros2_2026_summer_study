# ros2_2026_summer_study
2026 하계 ROS2 교육

---
## 8월3일(월) - 11일차

### Remind
#### 시뮬레이션 툴 Gazebo
    (1) world를 작성할 줄 알아야 함
    : model_name.sdf
    (2) 로봇 model을 작성할 줄 알아야 함
    : robot_name.sdf
    - 움직임 : topic 발행 & 구독
    - 센서 
    - 플러그인을 사용하여 원하는 동작 구현
    (3) spawn (gz create ..)
    : 원하는 센서 (카메라 등)를 추가
    (4) ROS2와 Gazebo 사이에서의 Bridge
    - use_sim_time을 활용해서 진행
    - 서로의 토픽이나 메시지들을 공유할 수 있음

### 1. 영상 처리
#### 영상처리란 무엇인가?
##### 1-1. 영상 처리
    입력된 영상을 어떤 목적을 위해 처리하는 기술
##### 1-2. 영상처리 수준
    (1) 저수준 영상처리
    : 영상 처리 결과가 영상인 경우
    (2) 고수준 영상처리
    : 영상 처리 결과가 영상이 아니라 영상의 특성을 나타내는 경우
##### 1-3. 영상의 형성 과정
    (1) 영상
    : 위치 값과 밝기 값을 가진 일정한 수의 화소들의 모임으로 정의함
    (2) 양자화
    : 제한된 비트수로 화소값을 나타내려 밝기 값을 정수화 시키는 과정
    (3) 샘플링
    : 무한한 연속된 값을 일정한 해상도에 따라 유한개의 화소수만큼 입력 값을 취하는 과정
##### 1-6. 디지털 영상의 표현과 영상 처리
##### M x N 크기 디저털 영상
    - 표본화 수에 따라 M, N을 결정
    - 양자화 수준에 따라 밝기 값 레벨 결정
    ① k 비트로 양자화 -> 2^(k)개 레벨
    ② 8비트 양자화 -> 2^(8)개 = 256개 레벨

### 2. OpenCV
#### 모듈이라 불리는 다수의 라이브러리들의 집합
    - core, dnn, photo, video 등등..
    - 1.0버전부터 다양한 버전이 존재
##### 2-1. OpenCV 관련 패키지
##### (1) Numpy
    $ pip install numpy
##### (2) Matplotlib
    $ pip install matplotlib
##### (3) Tensorflow
    $ pip install tensorflow
##### (4) PIL
    $ pip install pillow
##### (5) 기타
    - 유튜브 (youtube)
    $ pip install youtube_dl
    - 유튜브 메타 데이터
    $ pip install pafy
##### 2-2. 영상의 구조와 표현 방법
##### (1) 픽셀
    - 픽셀은 영상을 구성하는 최소 단위로 밝기 또는 색상을 표현함
    - 2차원으로 구성된 픽셀로 영상이 이루어짐
##### (2) Unisgned Char 자료형
    8 bit = 255 (레벨로 존재)
##### (3) 3 X Unsigned char 자료형
    8 bit X 3
##### 2-3. 영상 파일 형식
##### (1) BMP
    - BMP란 비트맵(bitmap) 이미지 파일 형식
    - 이미지를 픽셀 단위로 저장하는 래스터 그래픽 형식
    - 압축이 되어 있지 않아 용량이 큼
##### (2) JPG
    - JPG(Joint Photographic Experts Gruop) 래스터 그래픽 파일 형식
    - 압축이 되어 있기 때문에 이미지를 여러 번 저장하면 품질이 저하됨
    - 투명도 설정이 불가능하고 알차 채널 지원 X
    - 압축률 설정 가능

### 3. 이미지 띄우기
#### 원하는 이미지를 생성하거나 저장한 이미지를 띄움
##### 3-1. 버전 확인
    print("안녕하세요.")
    print(cv2.__version__)
##### 3-2. black 이미지 생성
##### 300x300 사이즈의 검은 이미지 생성
    def main():
        black_img = np.zeros((300, 300, 1), dtype=np.uint8)
        cv2.imshow("balck", black_img)
        cv2.waitKey()  # 블럭 함수
##### 3-3. 저장한 로봇 이미지 생성
##### 인터넷에서 저장한 robot 이미지를 불러옴
    def main():
        file_path = Path(__file__).parent
        img = cv2.imread("data/robot.jpg")  # 상대 경로
        img = cv2.imread(str(file_path / "data/robot.jpg"))  # 절대 경로
        cv2.imshow("robot", img) # 이미지를 보여줌
        cv2.waitKey()  # 블럭 함수
    
### 4. Camera_Opencv 패키지
#### camera_opencv 패키지를 생성하고 연습을 진행함
##### 4-1. img_show.py
##### 검정색 -> 하얀색으로 변하다가 키보드 'q' 입력시 종료되는 노드 생성
    class ImgShow(Node):
        def __init__(self):
            super().__init__("img_show_node") 
            self.create_timer(1 / 30, self.img_gen_callback)
            cv2.namedWindow("camera")
            self.img = np.zeros([300, 300], dtype=np.uint8)
            self.brightness = 0

        def img_gen_callback(self):
            self.brightness += 1
            self.img.fill(self.brightness)
            cv2.imshow("camera", self.img)
            if self.brightness > 255:
                self.brightness = 0
            key = cv2.waitKey(30)
            if key == ord("q"):
                raise KeyboardInterrupt
##### 4-2. reshape
##### img.reshape([500, 2000])을 통해 이미지의 크기를 변화시킴
    def main():
        file_path = Path(__file__).parent
        img = cv2.imread(str(file_path / "data/robot.jpg"), cv2.IMREAD_GRAYSCALE)
        print(type(img), img.shape, img.dtype)
        img = img.reshape([500, 2000])
        x = img.shape[1]
        y = img.shape[0]
        print(x, y)
        cv2.imshow("robot", img)
        cv2.waitKey()
##### 4-3. 변화시킨 이미지를 grayscale 후 저장
##### 용량이 적은 gray_10, 화질 좋은 일반 버전, 용량 큰 bmp 버전으로 저장해봄
    def main():
        file_path = Path(__file__).parent
        img = cv2.imread(str(file_path / "data/robot.jpg"), cv2.IMREAD_GRAYSCALE) 
        print(type(img), img.shape, img.dtype)
        img = img.reshape((500, 2000)) 
        x = img.shape[1]
        y = img.shape[0]
        print(x, y)
        cv2.imshow("robot", img)

        cv2.imwrite(str(file_path / "data" / "robot_gray.jpg"), img)
        imwrite_op = [cv2.IMWRITE_JPEG_QUALITY, 10]
        cv2.imwrite(str(file_path / "data" / "robot_gray_10.jpg"), img, imwrite_op)
        cv2.imwrite(str(file_path / "data" / "robot_gray.bmp"), img)
        cv2.waitKey() 
##### 4-4. 압축 Lib인 Compressed 활용
##### CompressedImage 라이브러리를 활용해 이미지 압축한 pub, sub 노드 생성
##### (1) compressed_pub
    def __init__(self):
        super().__init__('img_compressed_publisher')
        self.create_timer(1 / 30, self.img_gen_callback)
        cv2.namedWindow("camera")
        self.img = np.zeros([300, 300], dtype=np.uint8)
        self.brightness = 0
        self.pub = self.create_publisher(CompressedImage, "image_raw/compressed", 10)
        self.bridge = CvBridge()

    def img_gen_callback(self):
        self.brightness += 1
        self.img.fill(self.brightness)
        cv2.imshow("camera", self.img)
        if self.brightness > 255:
            self.brightness = 0
        key = cv2.waitKey(3)
        success, encoded_img =cv2.imencode(".jpg", self.img, [cv2.IMWRITE_JPEG_QUALITY, 25])
        if success:
            compressed_msg = CompressedImage()
            compressed_msg.header.stamp = self.get_clock().now().to_msg()
            compressed_msg.header.frame_id = "test img"
            compressed_msg.format = "jpeg"
            compressed_msg.data = encoded_img.tobytes()
            self.pub.publish(compressed_msg)
        else:
            self.get_logger().info("압축실패")
        if key == ord("q"):
            raise KeyboardInterrupt
##### (2) compressed_sub
    def __init__(self):
        super().__init__('img_compressed_subscriber')
        cv2.namedWindow("camera")
        self.bridge = CvBridge()
        self.create_subscription(CompressedImage, "image_raw/compressed", self.img_callback, 10)

    def img_callback(self, msg: CompressedImage):
        img = self.bridge.compressed_imgmsg_to_cv2(msg)
        cv2.imshow("camera", img)
        key = cv2.waitKey(3)
        if key == ord("q"):
            raise KeyboardInterrupt

### 5. MatplotLib 사용
#### matplotlib를 이용하여 다양한 예제 진행
##### 5-1. matplot으로 opencv로 읽어들인 이미지를 화면에 출력
##### opencv는 BGR, matplot은 RGB로 읽어들인다.
    def main():
        file_path = Path(__file__).parent
        img = cv2.imread(str(file_path / "data/robot.jpg"))
        cv2.imshow("robot", img)
        img.resize([400, 400, 3])
        plt.axis("off")
        imgRGB = cv2.cvtColor(img, cv2.COLOR_BGR2RGB) # OpenCV는 BGR, matplotlib는 RGB
        plt.imshow(imgRGB)
        plt.title("matplot complete")
        key = cv2.waitKey(30)
        plt.show()
    
### 6. 내장 카메라를 불러와 실행하기
#### 내장 카메라를 연결하고 불러와 화면에 띄움
##### 6-1. USB 연결
##### (1) bind
    $ usbipd bind --busid 1-6
##### (2) attach
    $ usbipd attach --wsl Ubuntu-24.04 --busid 1-6
##### (3) 확인
    $ usbipd list
    Attach 돼 있으면 성공
##### 6-2. MJPG 적용
    def main():
        file_path = Path(__file__).parent
        cap = cv2.VideoCapture(0, cv2.CAP_V4L2)
        # MJPG 설정
        fourcc = cv2.VideoWriter_fourcc(*"MJPG")
        cap.set(cv2.CAP_PROP_FOURCC, fourcc)
        
        cap.set(cv2.CAP_PROP_FRAME_WIDTH, 640)
        cap.set(cv2.CAP_PROP_FRAME_HEIGHT, 1480)
        cap.set(cv2.CAP_PROP_FPS, 30)
        
        
        if not cap.isOpened():
            return
        while True:
            ret, frame = cap.read()
            if not ret:
                break
            cv2.imshow("black", frame)
            if cv2.waitKey(1) == ord("q"):
                break
        cv2.destroyAllWindows()
##### 6-3. GSTREAMER 사용
    def main():
        file_path = Path(__file__).parent
        pipeline = (
            "v4l2src device=/dev/video0 !"
            "image/jpeg,width=1920,height=1080,framerate=30/1 !"
            "jpegdec ! "
            "videoconvert ! "
            "video/x-raw,format=BGR ! "
            "appsink drop=true sync=false"
        )
        cap = cv2.VideoCapture(pipeline, cv2.CAP_GSTREAMER)
        
        if not cap.isOpened():
            return
        while True:
            ret, frame = cap.read()
            if not ret:
                break
            cv2.imshow("Camera", frame)
            if cv2.waitKey(1) == ord("q"):
                break
        cv2.destroyAllWindows()

### 7. Ractangle, line
#### 사각형과 선을 그리는 예제
##### 7-1. lines
    def main():
        file_path = Path(__file__).parent
        white_img = np.full((600, 600, 1), 255, dtype=np.uint8)   # full은 일괄적으로 값을 넣어준다.
        pt1 = 100, 100

        diff = 0
        
        while True:
            white_img = np.full((600, 600, 1), 255, dtype=np.uint8)   # full은 일괄적으로 값을 넣어준다.
            diff += 1
            cv2.rectangle(white_img, pt1, (400 - diff, 400 - diff), (0, 255, 0), 2)
            cv2.line(white_img, (0,0), (500,0), (255, 0, 0), 5)
            cv2.line(white_img, (0,0), (0, 500), (0, 0, 255), 5)
            cv2.imshow("white", white_img)
            if cv2.waitKey(30) == ord("q"):
                break  
            cv2.destroyAllWindows()
##### 7-2. ractangle 함수
##### camera_pub.py 함수의 img_gen_callback()함수에 다음을 추가하면 사각 박스가 생성됨
    cv2.rectangle(frame, (10, 10), (self.width - 10, self.height - 10), (255, 0, 0), 3)
