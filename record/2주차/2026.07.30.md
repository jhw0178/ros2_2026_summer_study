# ros2_2026_summer_study
2026 하계 ROS2 교육

---
## 7월30일(목) - 9일차

### 1. 미니 프로젝트
#### 구역을 6개로 나누는 벽을 설치하고, 각 구역을 들르며 물체를 옮긴다. 각 구역에서 BOX를 attach, detach를 해도 되고, 안 해도 된다.
##### 1-1. 6개의 구역을 만들도록 벽 생성
    def add_wall(self):
        center_x = 0.0
        center_y = 0.0

        inner_radius = 0.12    # 중심부 빈 공간 확보 (반경 12cm까지는 벽 없음)
        wall_length = 0.25     # 벽의 길이
        wall_thickness = 0.02
        wall_height = 0.04     # 로봇이 넘나들 수 있는 낮은 높이

        for i in range(6):
            angle = math.radians(i * 60)

            collision_object = CollisionObject()
            collision_object.header.frame_id = "world"
            collision_object.id = f"wall_{i}"

            wall = SolidPrimitive()
            wall.type = SolidPrimitive.BOX
            wall.dimensions = [wall_length, wall_thickness, wall_height]

            pose = Pose()

            # 벽의 중심 좌표: 빈 공간(inner_radius) + 벽 길이의 절반
            offset = inner_radius + (wall_length / 2.0)
            pose.position.x = center_x + offset * math.cos(angle)
            pose.position.y = center_y + offset * math.sin(angle)
            pose.position.z = wall_height / 2.0

            # 벽 회전
            pose.orientation.z = math.sin(angle / 2.0)
            pose.orientation.w = math.cos(angle / 2.0)

            collision_object.primitives.append(wall)
            collision_object.primitive_poses.append(pose)
            collision_object.operation = CollisionObject.ADD

            self.planning_scene_monitor.process_collision_object(collision_object)

        self.get_logger().info("중앙이 빈 피자 조각 모양의 방사형 벽 6개 생성 완료")

##### 1-2. 초기 박스 생성 및 위치 설정
    def add_pick_object(self):
        collision_object = CollisionObject()
        collision_object.header.frame_id = "world"
        collision_object.header.stamp = self.get_clock().now().to_msg()
        collision_object.id = self.object_id
        
        box = SolidPrimitive()
        box.type = SolidPrimitive.BOX
        box.dimensions = [0.04, 0.04, 0.08]
        
        box_pose = Pose()
        box_pose.position.x = 0.0
        box_pose.position.y = -0.3
        box_pose.position.z = 0.065
        box_pose.orientation.w = 1.0
        
        collision_object.primitives.append(box)                 # type: ignore
        collision_object.primitive_poses.append(box_pose)       # type: ignore
        collision_object.operation = CollisionObject.ADD
        
        success = self.planning_scene_monitor.process_collision_object(collision_object)
        if success:
            self.get_logger().info("world에 box 추가 성공")
        return successdef add_pick_object(self):
        collision_object = CollisionObject()
        collision_object.header.frame_id = "world"
        collision_object.header.stamp = self.get_clock().now().to_msg()
        collision_object.id = self.object_id
        
        box = SolidPrimitive()
        box.type = SolidPrimitive.BOX
        box.dimensions = [0.04, 0.04, 0.08]
        
        box_pose = Pose()
        box_pose.position.x = 0.0
        box_pose.position.y = -0.3
        box_pose.position.z = 0.065
        box_pose.orientation.w = 1.0
        
        collision_object.primitives.append(box)                 # type: ignore
        collision_object.primitive_poses.append(box_pose)       # type: ignore
        collision_object.operation = CollisionObject.ADD
        
        success = self.planning_scene_monitor.process_collision_object(collision_object)
        if success:
            self.get_logger().info("world에 box 추가 성공")
        return success
##### 1-3. 시작 위치 및 이동 경로 관절 값 설정
    def move_manipulator(self):
        #gripper open
        self.plan_and_execute(
                    self.moveit,
                    self.gripper,
                    configuration="open",
                    controller_name="gripper_controller",
                )
        # 물체 잡기 직전 자세
        self.plan_and_execute(
            self.moveit,
            self.arm,
            configuration={
                "joint1": 1.5800002115220035,
                "joint2": -1.3560390164911138,
                "joint3": -1.1382137446113525,
                "joint4": -1.5846021538860742,
            },
            controller_name="arm_controller",
        )
        
        # gripper 닫기
        self.plan_and_execute(
            self.moveit,
            self.gripper,
            configuration="close",
            controller_name="gripper_controller",
        )
        # 물체를 로봇 부착
        self.attach_object()
        # # 물체를 든 상태로 이동1
        self.plan_and_execute(
            self.moveit,
            self.arm,
            configuration={
                "joint1": -1.0707185899443843,
                "joint2": 0.45405831321394263,
                "joint3": 0.1978835216370407,
                "joint4": -0.8789709914586794,
            },
            controller_name="arm_controller",
        )
        
        # # 물체를 든 상태로 이동2
        self.plan_and_execute(
            self.moveit,
            self.arm,
            configuration={
                "joint1": -0.5460971604874953,
                "joint2": 0.7869321441851271,
                "joint3": -0.0506213660004331,
                "joint4": -0.7378447589732002,
            },
            controller_name="arm_controller",
        )
        # # 물체를 든 상태로 이동3
        self.plan_and_execute(
            self.moveit,
            self.arm,
            configuration={
                "joint1": 0.05215534678790501,
                "joint2": 0.37735927381966095,
                "joint3": 0.4141748127289162,
                "joint4": -0.9142525495800489,
            },
            controller_name="arm_controller",
        )

### 2. 가상환경
#### 시뮬레이션을 위한 가상환경
##### Gazebo
    (1) Class Gazebo
    (2) Ignition Gazebo
##### Nvdia
    (1) Issaac Sim
##### Game Engine 
    (1) Unity
    (2) un real engine
##### 2-1. Gazebo 설치
##### https://gazebosim.org/docs/harmonic/ros_installation/ 방문하여 환경에 맞게 설치
    $ sudo apt-get install ros-${ROS_DISTRO}-ros-gz
##### 2-2. Gazebo 실행
    $ ga zim

### 3. 시뮬레이션 실행 준비 및 검증
### Gate A. 기본 환경
#### 입력 코드
    $ source /opt/ros/jazzy/setup.bash

    $ printenv ROS_DISTRO
    $ ros2 pkg prefix rclpy
    $ sudo apt install mesa-utils
    $ glxinfo -B
    $ rviz2
##### 통과 기준
    - ROS_DISTRO=jazzy
    - rclpy 경로가 /opt/ros/jazzy 아래에서 확인됨
    - WSLg에서 OpenGL renderer가 확인되고 RViz2가 검은 화면 없이 열림
    - OpenGL renderer string: D3D12 (AMD Radeon(TM) Graphics) d3d12 스트링 확인
##### 실패 조치
    - Ubuntu 24.04와 ROS 2 Jazzy 조합을 먼저 복구한다.
    - GUI 문제는 Windows GPU 드라이버, WSL 업데이트, WSL 메모리 설정을 확인한다.
### Gate B. OpenMANIPULATOR 소스와 빌드
#### 입력 코드
    $ cd ~/ros2_2026_summer_study/open_manipulator_ws
    $ source /opt/ros/jazzy/setup.bash

    $ sudo rosdep init          # 최초 1회만 실행
    $ rosdep update
    $ rosdep install --from-paths src -y --ignore-src

    $ colcon build --symlink-install --cmake-args -DCMAKE_BUILD_TYPE=Release
    $ source install/setup.bash
    $ source ~/ros2_2026_summer_study/open_manipulator_ws/install/local_setup.bash

    $ ros2 pkg prefix open_manipulator_bringup
    $ ros2 pkg prefix open_manipulator_moveit_config
    $ os2 pkg prefix open_manipulator_gui
    $ ros2 pkg executables open_manipulator_teleop
##### 통과 기준
    - 빌드 실패 패키지 0개
    - bringup, MoveIt config, GUI, teleop 패키지 또는 실행 파일 확인
    - 새 터미널에서도 두 setup 파일을 source한 뒤 같은 결과 확인
##### 실패 조치
    - rosdep 오류와 CMake 오류를 분리해 기록한다.
    - Humble 패키지나 과거 open_manipulator_x_* 패키지가 overlay에 섞이지 않았는지 확인한다.
    - 직접 설치가 교육생 PC별로 불안정하면 ROBOTIS 공식 Jazzy Docker 흐름을 대체 경로로 검증한다.
### Gate C. Description·TF·RViz2
#### 입력 코드
    $ source /opt/ros/jazzy/setup.bash
    $ source ~/ros2_2026_summer_study/open_manipulator_ws/install/setup.bash

    $ ros2 launch open_manipulator_description open_manipulator_x.launch.py
#### 별도 터미널
    $ ros2 topic echo /joint_states --once
    $ ros2 run tf2_tools view_frames
##### 통과 기준
    - RViz2에 모델과 mesh가 누락 없이 표시됨
    - Fixed Frame 오류 없음
    - /joint_states와 base에서 end-effector까지의 TF chain 확인
##### 실패 조치
    - package share의 mesh 경로, Xacro 파라미터, robot_description, Fixed Frame을 순서대로 확인한다.
### Gate D. Gazebo Harmonic 시뮬레이션
#### 입력 코드
    $ sudo apt-get install ros-${ROS_DISTRO}-ros-gz

    $ source /opt/ros/jazzy/setup.bash
    $ source ~/ros2_2026_summer_study/open_manipulator_ws/install/setup.bash

    $ gz sim --versions
    $ ros2 pkg prefix ros_gz_sim
    $ ros2 launch open_manipulator_bringup open_manipulator_x_gazebo.launch.py
#### 별도 터미널
    $ ros2 topic echo /clock --once
    $ ros2 topic echo /joint_states --once
    $ ros2 control list_controllers
##### 통과 기준
    - Gazebo Harmonic 실행 및 로봇 모델 표시
    - Play 상태에서 /clock 증가
    - /joint_states 수신
    - broadcaster와 trajectory controller가 기대 상태로 표시
##### 실패 조치
    - Gazebo Classic 명령이나 gazebo_ros 패키지를 사용하지 않았는지 확인한다.
    - 창만 열린 경우 Play, controller state, /clock, bridge 순서로 확인한다.
    - 검은 화면이면 OpenGL renderer와 WSL 자원 할당을 확인한다.
### Gate E. MoveIt2·teleop·GUI 시뮬레이션
#### 입력 코드
#### 터미널 1
    $ ros2 launch open_manipulator_bringup open_manipulator_x_gazebo.launch.py
#### 터미널 2
    $ ros2 launch open_manipulator_moveit_config open_manipulator_x_moveit.launch.py use_sim:=true
#### 터미널 3
    $ ros2 run open_manipulator_teleop open_manipulator_x_teleop
    또는
    $ ros2 launch open_manipulator_gui open_manipulator_x_gui.launch.py
##### 통과 기준
    - MoveIt 2에서 현재 상태가 정상으로 표시됨
    - home 등 안전한 Named State 계획 성공
    - 계획 실행 후 Gazebo 관절과 /joint_states가 함께 변경됨
    - arm과 gripper를 각각 제어 가능
##### 실패 조치
    - teleop과 GUI를 동시에 명령원으로 사용하지 않는다.
    - MoveIt start state, controller 이름, action server, simulation Play 상태를 확인한다.
### Gate F. WSL USB와 실기기
#### 입력 코드
#### Windows 관리자 Powershell
    $ usbipd list
    $ usbipd bind --busid <BUSID>
    $ usbipd attach --wsl --busid <BUSID>
#### WSL
    $ lsusb
    $ ls -l /dev/ttyUSB* /dev/ttyACM*
    $ groups
    # 필요한 경우
    $ sudo usermod -aG dialout $USER
#### 실기기 실행 전 체크
    (1) 로봇 전원 OFF 상태에서 케이블과 관절 시작 자세 확인
    (2) 작업 반경에서 사람과 물체 제거
    (3) 비상 정지 또는 토크 해제 절차 확인
    (4) 포트가 U2D2인지 OpenCR인지 확인
    (5) 첫 동작은 한 관절, 낮은 속도, 작은 범위로 제한
#### 공식 Jazzy 운영 흐름
    $ source /opt/ros/jazzy/setup.bash
    $ source ~/open_manipulator_ws/install/setup.bash

    $ ros2 launch open_manipulator_bringup open_manipulator_x.launch.py
#### 별도 터미널
    $ ros2 topic echo /joint_states --once
    $ ros2 control list_controllers
#### 그 다음 MoveIt2
    $ ros2 launch open_manipulator_moveit_config open_manipulator_x_moveit.launch.py
##### 통과 기준
    - DYNAMIXEL 통신 오류 없이 bringup 완료
    - 실제 관절을 손으로 움직일 때 /joint_states가 일관되게 변함
    - 안전 Named State의 계획과 저속 실행 성공
    - gripper open/close 성공
##### 실패 조치
    - 자세 범위, 전원, 포트, 권한, USB attach, DYNAMIXEL ID와 firmware를 차례로 확인한다.
    - 실기기 문제를 해결하는 동안 수업은 Gate E 시뮬레이션으로 진행한다.

### 4. Gazebo
##### 3-1. 기본 설정
##### source 설정
    source /opt/ros/jazzy/setup.bash
    source ~/open_manipulator_ws/install/setup.bash
##### 버전 확인
    $ gz sim --versions
##### Gazebo 버전 확인
    $ ros2 pkg prefix ros_gz_sim

    sudo apt-get install ros-${ROS_DISTRO}-ros-gz
##### 3-2. world 생성
##### tf2_basic 패키지에 world 폴더 생성
    empty_world.sdf 파일을 생성하고
    open_manipulator_bringup의 worlds 파일의 empty_world.sdf 파일 복사 후 붙여넣기
##### 내가 만든 .sdf 파일로 gazebo 실행
    $ gz sim ~/ros2_2026_summer_study/ros_ws/src/tf2_basic/world/empty_world.sdf
##### gazebo 안에서 추가 혹은 sdf 파일 수정하여 원하는 물체 추가
    <model name='box'>
      <pose>2.5066160933673487 2.4517091912773994 0.5 0 0 0</pose>
      <link name='box_link'>
        <inertial>
          <inertia>
            <ixx>0.16666</ixx>
            <ixy>0</ixy>
            <ixz>0</ixz>
            <iyy>0.16666</iyy>
            <iyz>0</iyz>
            <izz>0.16666</izz>
          </inertia>
          <mass>1</mass>
          <pose>0 0 0 0 0 0</pose>
        </inertial>
        <collision name='box_collision'>
          <geometry>
            <box>
              <size>1 1 1</size>
            </box>
          </geometry>
          <surface>
            <friction>
              <ode/>
            </friction>
            <bounce/>
            <contact/>
          </surface>
        </collision>
        <visual name='box_visual'>
          <geometry>
            <box>
              <size>0.2 10 1</size>
            </box>
          </geometry>
          <material>
            <ambient>1 0 0 1</ambient>
            <diffuse>1 0 0 1</diffuse>
            <specular>1 1 1 1</specular>
          </material>
        </visual>
        <pose>0 0 0 0 0 0</pose>
        <enable_wind>false</enable_wind>
      </link>
      <static>false</static>
      <self_collide>false</self_collide>
    </model>

##### 3-3. 모델 불러오기
##### https://app.gazebosim.org/dashboard 사이트에서 원하는 모델의 .sdf 파일 가져와 실행
    $ gz sim ~/ros2_2026_summer_study/ros_ws/src/tf2_basic/world/"가져온파일.sdf"

##### 3-4. open_manipulator_x gazebo로 실행
    $ ros2 launch open_manipulator_bringup open_manipulator_x_gazebo.launch.py
##### 3-5. open_manipulator_x 키보드로 조종하기
##### open_manipulator_teleop을 이용해 조종해보자
##### 1/q,2/w,3/e,4/r,o/p를 이용해 Joint 및 gripper를 조절할 수 있음
    $ ros2 run open_manipulator_teleop open_manipulator_x_teleop.py

### 4. aruco 이미지 추가
#### aruco 이미지를 추가한 box를 생성하는 연습
##### 4-1. 전체적인 tree
    ~/ros2_2026_summer_study/ros_ws/src/tf2_basic/models
    └── aruco_cube
        ├── material
        │   └── textures
        │       ├── aruco_0.png
        │       ├── aruco_1.png
        │       └── create_aruco_marker.py
        ├── model.config
        └── model.sdf
##### 4-2. model.sdf
    <?xml version="1.0" ?>
    <sdf version='1.10'>
    <model name='aruco_cube'>
        <link name="cube_link">
            <collision name="cube_collision">
                <geometry>
                    <box>
                    <size>0.05 0.05 0.05</size>
                    </box>
                </geometry>
            </collision>

            <visual name="cube_visual">
                <geometry>
                    <box>
                    <size>0.05 0.05 0.05</size>
                    </box>
                </geometry>
                <material>
                    <ambient>1 1 1 1</ambient>
                    <diffuse>1 1 1 1</diffuse>
                    <specular>0.1 0.1 0.1 1</specular>
                </material>
            </visual>

            <visual name="aruco_top_visual">
                <pose>0 0 0.0251 0 0 0</pose>
                <geometry>
                    <box>
                        <size>0.04 0.04 0.0001</size>
                    </box>
                </geometry>
                <material>
                    <ambient>1 1 1 1</ambient>
                    <diffuse>1 1 1 1</diffuse>
                    <pbr>
                        <metal>
                            <albedo_map>
                                material/textures/aruco_0.png
                            </albedo_map>
                            <roughness>1.0</roughness>
                            <metalness>0.0</metalness>
                        </metal>
                    </pbr>
                </material>
            </visual>

        </link>
    </model>
    </sdf>
##### 4-3. create_aruco_marker.py
##### aruco.png를 생성하는 excutor
    import cv2

    print(cv2.__version__)

    dictionary = cv2.aruco.getPredefinedDictionary(cv2.aruco.DICT_4X4_50)

    marker_id = 1
    marker_size = 1000

    marker = cv2.aruco.drawMarker(
        dictionary,
        marker_id,
        marker_size,
    )

    marker_with_margin = cv2.copyMakeBorder(
        marker,
        100,
        100,
        100,
        100,
        cv2.BORDER_CONSTANT,
        value=255,
    )

    output_path = "aruco_0.png"

    success = cv2.imwrite(output_path, marker_with_margin)

    if not success:
        raise RuntimeError(f"이미지 저장 실패: {output_path}")

    print(f"{output_path} 생성 완료")
##### 원하는 번호를 붙이고 싶다면 output_path="aruco_"원하는번호".png"로 수정하면 된다.
##### 이후 경로 이동 후 파이썬 파일을 실행하면 됨
    $ cd ~/ros2_2026_summer_study/ros_ws/src/tf2_basic/models/aruco_cube/material/textures
    $ python3 create_aruco_marker.py
##### 4-4. empty_world에 추가
##### 생성된 model을 tf2_basic 패키지의 empty_world.sdf에 추가
    <include>
      <uri>model://aruco_cube</uri>
      <name>aruco_cube_0</name>
      <pose>0 0 0 0 0 0</pose>
    </include>
##### 4-5. acruco_cube가 추가된 empty_world
    $ gz sim ~/ros2_2026_summer_study/ros_ws/src/tf2_basic/world/empty_world.sdf
##### 4-6. 물체에 마찰력 강도 설정하기
##### 값이 높아질수록 마찰력이 강해짐
    <surface>
        <friction>
            <ode>
                <mu>1.5</mu>
                <mu2>1.5</mu2>
            </ode>
        </friction>
    </surface>
##### 이렇게 마찰력을 높이고 시뮬레이션해보면, 직접 물체를 잡을 수 있음